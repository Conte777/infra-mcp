# local tool binaries installed by `make tools`
/bin/

# build output
/dist/

# go test artifacts
*.test
*.out

# local config with secrets substituted in
*.local.json

# per-developer hook overrides
/lefthook-local.yml

.DS_Store

# a global ignore hides these; this repo keeps its agent instructions in tree
!CLAUDE.md
!.claude/
# ...but not the worktrees Claude Code checks out under it
/.claude/worktrees/
